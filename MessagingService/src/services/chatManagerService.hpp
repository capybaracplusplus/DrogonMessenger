#pragma once

#include "../models/message.hpp"

class ChatManagerService {
public:
    static void writeMessage(const Message & message) {
        // проверить что такой чат существует
        // проверить что такой получатель существует
    }
};